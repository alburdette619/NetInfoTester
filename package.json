{
  "name": "NetInfoTester",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "ios": "react-native run-ios",
    "ios-device": "react-native run-ios --device",
    "android": "react-native run-android",
    "clean": "rm -rf node_modules && yarn && watchman watch-del-all && react-native start --reset-cache"
  },
  "dependencies": {
    "react": "16.2.0",
    "react-native": "^0.52.0-rc.0"
    "expo": "^24.0.2",
    "react": "16.0.0",
    "react-native": "0.51.0"
  },
  "devDependencies": {
    "babel-jest": "22.0.4",
    "babel-preset-react-native": "4.0.0",
    "jest": "22.0.4",
    "react-test-renderer": "16.0.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
